<h1>Edit user</h1>

<%= form_for(@user) do |f| %>
  
  <%= render 'fields', :f => f %>
  <%= render 'admin_fields', :f => f %>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>
